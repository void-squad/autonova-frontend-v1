# Backend API Configuration
VITE_API_BASE_URL=http://localhost:8080
# Optional: direct project-service API for local dev (falls back to gateway if empty)
VITE_PROJECT_API_BASE_URL=http://localhost:8082
# Optional: override only the port if you access the dev server via IP (defaults to 8082)
# VITE_PROJECT_API_PORT=8082
